<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Anime List</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        :root {
            --primary: #7c3aed;
            --primary-dark: #6d28d9;
            --secondary: #f472b6;
            --accent: #10b981;
            --dark: #1f2937;
            --light: #f9fafb;
            --gray: #6b7280;
            --card-bg: #ffffff;
            --shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #e4e9f2 100%);
            min-height: 100vh;
            color: var(--dark);
            position: relative;
        }

        /* Background decorative elements */
        .bg-blob {
            position: fixed;
            width: 500px;
            height: 500px;
            border-radius: 50%;
            opacity: 0.1;
            z-index: -1;
        }

        .blob-1 {
            background: radial-gradient(circle, var(--primary) 0%, transparent 70%);
            top: -200px;
            right: -100px;
            width: 600px;
            height: 600px;
        }

        .blob-2 {
            background: radial-gradient(circle, var(--secondary) 0%, transparent 70%);
            bottom: -150px;
            left: -100px;
            width: 400px;
            height: 400px;
        }

        /* Navbar */
        .navbar {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            color: var(--dark);
            padding: 18px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: var(--shadow);
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            width: 40px;
            height: 40px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 20px;
            transform: rotate(-10deg);
        }

        .logo h1 {
            font-size: 24px;
            font-weight: 700;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .navbar .user-info {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
            font-size: 18px;
        }

        .navbar a {
            color: white;
            text-decoration: none;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: 600;
            transition: var(--transition);
            box-shadow: 0 4px 6px rgba(124, 58, 237, 0.2);
        }

        .navbar a:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(124, 58, 237, 0.3);
        }

        /* Container */
        .container {
            max-width: 1300px;
            margin: 40px auto;
            padding: 0 20px;
        }

        /* Stats Cards */
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: var(--card-bg);
            padding: 25px;
            border-radius: 16px;
            box-shadow: var(--shadow);
            display: flex;
            align-items: center;
            gap: 20px;
            transition: var(--transition);
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        }

        .stat-icon {
            width: 60px;
            height: 60px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            color: white;
        }

        .stat-1 .stat-icon {
            background: linear-gradient(135deg, #667eea, #764ba2);
        }

        .stat-2 .stat-icon {
            background: linear-gradient(135deg, #f093fb, #f5576c);
        }

        .stat-3 .stat-icon {
            background: linear-gradient(135deg, #4facfe, #00f2fe);
        }

        .stat-4 .stat-icon {
            background: linear-gradient(135deg, #43e97b, #38f9d7);
        }

        .stat-info h3 {
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 5px;
        }

        .stat-info p {
            color: var(--gray);
            font-size: 14px;
        }

        /* Add Anime Form */
        .add-form {
            background: var(--card-bg);
            padding: 30px;
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-bottom: 40px;
            transition: var(--transition);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .add-form:hover {
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        }

        .form-header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 25px;
        }

        .form-header i {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            font-size: 24px;
        }

        .add-form h2 {
            color: var(--dark);
            font-size: 22px;
            font-weight: 700;
        }

        .form-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            align-items: end;
        }

        .form-group {
            margin-bottom: 0;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: var(--dark);
            font-weight: 600;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .form-group label i {
            color: var(--primary);
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 14px;
            border: 2px solid #e5e7eb;
            border-radius: 10px;
            font-size: 15px;
            transition: var(--transition);
            background-color: #f9fafb;
        }

        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: var(--primary);
            background-color: white;
            box-shadow: 0 0 0 3px rgba(124, 58, 237, 0.1);
        }

        .file-upload {
            position: relative;
            overflow: hidden;
        }

        .file-upload input[type="file"] {
            position: absolute;
            top: 0;
            left: 0;
            opacity: 0;
            width: 100%;
            height: 100%;
            cursor: pointer;
        }

        .file-upload-label {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            background: #f3f4f6;
            border: 2px dashed #d1d5db;
            border-radius: 10px;
            padding: 14px;
            cursor: pointer;
            transition: var(--transition);
            color: var(--gray);
        }

        .file-upload-label:hover {
            border-color: var(--primary);
            background: #f9fafb;
            color: var(--primary);
        }

        .btn-submit {
            padding: 14px 30px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            box-shadow: 0 4px 6px rgba(124, 58, 237, 0.2);
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(124, 58, 237, 0.3);
        }

        /* Anime Grid */
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }

        .section-title {
            font-size: 24px;
            font-weight: 700;
            color: var(--dark);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .section-title i {
            color: var(--primary);
        }

        .anime-count {
            background: var(--primary);
            color: white;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
        }

        .anime-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 30px;
        }

        .anime-card {
            background: var(--card-bg);
            border-radius: 16px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: var(--transition);
            position: relative;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .anime-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        }

        .anime-image-container {
            height: 320px;
            overflow: hidden;
            position: relative;
        }

        .anime-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .anime-card:hover .anime-image {
            transform: scale(1.05);
        }

        .anime-rating-badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 8px 12px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 5px;
            backdrop-filter: blur(5px);
        }

        .anime-info {
            padding: 20px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .anime-title {
            font-size: 18px;
            font-weight: 700;
            color: var(--dark);
            margin-bottom: 10px;
            line-height: 1.4;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .anime-meta {
            color: var(--gray);
            font-size: 14px;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .anime-genre {
            background: #f3f4f6;
            color: var(--primary);
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            margin-top: 5px;
            align-self: flex-start;
        }

        .anime-actions {
            display: flex;
            gap: 10px;
            margin-top: 20px;
        }

        .btn-edit, .btn-delete {
            flex: 1;
            padding: 10px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 14px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn-edit {
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
            color: white;
            text-decoration: none;
            text-align: center;
        }

        .btn-edit:hover {
            background: linear-gradient(135deg, #1d4ed8, #1e40af);
            transform: translateY(-2px);
        }

        .btn-delete {
            background: linear-gradient(135deg, #ef4444, #dc2626);
            color: white;
            border: none;
        }

        .btn-delete:hover {
            background: linear-gradient(135deg, #dc2626, #b91c1c);
            transform: translateY(-2px);
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 20px;
            color: var(--gray);
            background: var(--card-bg);
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-top: 20px;
        }

        .empty-icon {
            font-size: 80px;
            margin-bottom: 20px;
            color: #d1d5db;
        }

        .empty-state h3 {
            font-size: 24px;
            margin-bottom: 10px;
            color: var(--dark);
        }

        .empty-state p {
            font-size: 16px;
            max-width: 500px;
            margin: 0 auto 30px;
            line-height: 1.6;
        }

        .btn-add-first {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            text-decoration: none;
            padding: 12px 30px;
            border-radius: 10px;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: var(--transition);
            box-shadow: 0 4px 6px rgba(124, 58, 237, 0.2);
        }

        .btn-add-first:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(124, 58, 237, 0.3);
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 30px 20px;
            color: var(--gray);
            font-size: 14px;
            margin-top: 50px;
            border-top: 1px solid rgba(0, 0, 0, 0.05);
        }

        .footer a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
        }

        /* Responsive */
        @media (max-width: 1100px) {
            .form-row {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .navbar {
                padding: 15px 20px;
            }

            .logo h1 {
                font-size: 20px;
            }

            .form-row {
                grid-template-columns: 1fr;
            }

            .anime-grid {
                grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            }

            .stats-container {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 576px) {
            .navbar {
                flex-direction: column;
                gap: 15px;
            }

            .section-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 15px;
            }

            .stats-container {
                grid-template-columns: 1fr;
            }

            .anime-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Animation for new elements */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .animate-in {
            animation: fadeIn 0.5s ease forwards;
        }

        /* Scrollbar styling */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }

        ::-webkit-scrollbar-thumb {
            background: var(--primary);
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--primary-dark);
        }
    </style>
</head>

<body>
    <!-- Background decorative elements -->
    <div class="bg-blob blob-1"></div>
    <div class="bg-blob blob-2"></div>

    <!-- Navbar -->
    <div class="navbar animate-in">
        <div class="logo">
            <div class="logo-icon">
                <i class="fas fa-film"></i>
            </div>
            <h1>My Anime Dashboard</h1>
        </div>
        <div class="user-info">
            <div class="user-avatar">
                <%= username.charAt(0).toUpperCase() %>
            </div>
            <span>Welcome, <strong><%= username %></strong></span>
            <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
    </div>

    <div class="container">
        <!-- Stats Cards -->
        <div class="stats-container animate-in" style="animation-delay: 0.1s">
            <div class="stat-card stat-1">
                <div class="stat-icon">
                    <i class="fas fa-tv"></i>
                </div>
                <div class="stat-info">
                    <h3><%= animeList.length %></h3>
                    <p>Total Anime</p>
                </div>
            </div>
            <div class="stat-card stat-2" style="animation-delay: 0.2s">
                <div class="stat-icon">
                    <i class="fas fa-star"></i>
                </div>
                <div class="stat-info">
                    <h3><%= animeList.length > 0 ? (animeList.reduce((sum, anime) => sum + anime.rating, 0) / animeList.length).toFixed(1) : '0.0' %></h3>
                    <p>Average Rating</p>
                </div>
            </div>
            <div class="stat-card stat-3" style="animation-delay: 0.3s">
                <div class="stat-icon">
                    <i class="fas fa-play-circle"></i>
                </div>
                <div class="stat-info">
                    <h3><%= animeList.length > 0 ? animeList.reduce((sum, anime) => sum + (anime.episodes || 0), 0) : 0 %></h3>
                    <p>Total Episodes</p>
                </div>
            </div>
            <div class="stat-card stat-4" style="animation-delay: 0.4s">
                <div class="stat-icon">
                    <i class="fas fa-heart"></i>
                </div>
                <div class="stat-info">
                    <h3><%= animeList.filter(a => a.rating >= 8).length %></h3>
                    <p>Top Rated (8+)</p>
                </div>
            </div>
        </div>

        <!-- Add Anime Form -->
        <div class="add-form animate-in" style="animation-delay: 0.5s">
            <div class="form-header">
                <i class="fas fa-plus-circle"></i>
                <h2>Tambah Anime Baru</h2>
            </div>
            <form method="POST" action="/anime/add" enctype="multipart/form-data">
                <div class="form-row">
                    <div class="form-group">
                        <label><i class="fas fa-heading"></i> Judul Anime</label>
                        <input type="text" name="title" required placeholder="Contoh: Attack on Titan">
                    </div>
                    <div class="form-group">
                        <label><i class="fas fa-list-ol"></i> Episode</label>
                        <input type="number" name="episodes" min="1" placeholder="12">
                    </div>
                    <div class="form-group">
                        <label><i class="fas fa-tags"></i> Genre</label>
                        <input type="text" name="genre" placeholder="Action, Fantasy, Shounen">
                    </div>
                    <div class="form-group">
                        <label><i class="fas fa-star"></i> Rating (1-10)</label>
                        <input type="number" name="rating" min="1" max="10" required placeholder="9">
                    </div>
                    <div class="form-group file-upload">
                        <label><i class="fas fa-image"></i> Upload Gambar</label>
                        <div class="file-upload-label">
                            <i class="fas fa-cloud-upload-alt"></i>
                            <span>Pilih gambar...</span>
                        </div>
                        <input type="file" name="image" accept="image/*">
                    </div>
                    <button type="submit" class="btn-submit">
                        <i class="fas fa-plus"></i> Tambah Anime
                    </button>
                </div>
            </form>
        </div>

        <!-- Anime List -->
        <div class="section-header animate-in" style="animation-delay: 0.6s">
            <div class="section-title">
                <i class="fas fa-list"></i> Daftar Anime
                <span class="anime-count"><%= animeList.length %></span>
            </div>
        </div>

        <% if (animeList.length === 0) { %>
            <div class="empty-state animate-in" style="animation-delay: 0.7s">
                <div class="empty-icon">
                    <i class="fas fa-film"></i>
                </div>
                <h3>Belum ada anime nih 😢</h3>
                <p>Mulai dengan menambahkan anime favorit kamu ke dalam daftar. Klik tombol "Tambah Anime" di atas atau tombol di bawah ini.</p>
                <a href="#" class="btn-add-first" onclick="document.querySelector('.add-form').scrollIntoView({ behavior: 'smooth' }); return false;">
                    <i class="fas fa-plus"></i> Tambah Anime Pertama
                </a>
            </div>
        <% } else { %>
            <div class="anime-grid">
                <% animeList.forEach((anime, index) => { %>
                    <div class="anime-card animate-in" style="animation-delay: <%= 0.7 + (index * 0.1) %>s">
                        <div class="anime-image-container">
                            <% if (anime.image_path) { %>
                                <img src="/uploads/<%= anime.image_path %>" alt="<%= anime.title %>" class="anime-image">
                            <% } else { %>
                                <div class="anime-image" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); display: flex; align-items: center; justify-content: center; color: white; font-size: 60px;">
                                    <i class="fas fa-film"></i>
                                </div>
                            <% } %>
                            <div class="anime-rating-badge">
                                <i class="fas fa-star"></i> <%= anime.rating %>/10
                            </div>
                        </div>
                        <div class="anime-info">
                            <div class="anime-title" title="<%= anime.title %>">
                                <%= anime.title %>
                            </div>
                            
                            <% if (anime.episodes) { %>
                                <div class="anime-meta">
                                    <i class="fas fa-play-circle"></i> <%= anime.episodes %> Episode
                                </div>
                            <% } %>
                            
                            <% if (anime.genre) { %>
                                <div class="anime-genre">
                                    <%= anime.genre.split(',')[0].trim() %>
                                </div>
                            <% } %>
                            
                            <div class="anime-actions">
                                <a href="/anime/edit/<%= anime.id %>" class="btn-edit">
                                    <i class="fas fa-edit"></i> Edit
                                </a>
                                <form method="POST" action="/anime/delete/<%= anime.id %>" style="display: inline; width: 100%;">
                                    <button type="submit" class="btn-delete" onclick="return confirm('Yakin ingin menghapus anime ini?')">
                                        <i class="fas fa-trash"></i> Hapus
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>

    <!-- Footer -->
    <div class="footer animate-in" style="animation-delay: 0.8s">
        <p>© 2023 My Anime Dashboard. Dibuat dengan <i class="fas fa-heart" style="color: #ef4444;"></i> untuk penggemar anime.</p>
        <p>Total <%= animeList.length %> anime di database | Terakhir diperbarui: <span id="current-date"></span></p>
    </div>

    <script>
        // Set current date in footer
        const now = new Date();
        const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
        document.getElementById('current-date').textContent = now.toLocaleDateString('id-ID', options);

        // File upload label update
        document.querySelectorAll('input[type="file"]').forEach(input => {
            input.addEventListener('change', function() {
                const label = this.closest('.file-upload').querySelector('.file-upload-label span');
                if (this.files.length > 0) {
                    label.textContent = this.files[0].name;
                } else {
                    label.textContent = 'Pilih gambar...';
                }
            });
        });

        // Add hover effect to stat cards
        document.querySelectorAll('.stat-card').forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-5px)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0)';
            });
        });

        // Smooth scroll to form when clicking "Tambah Anime Pertama"
        document.querySelectorAll('a[href="#"]').forEach(link => {
            link.addEventListener('click', function(e) {
                if (this.getAttribute('onclick') && this.getAttribute('onclick').includes('scrollIntoView')) {
                    e.preventDefault();
                    document.querySelector('.add-form').scrollIntoView({ 
                        behavior: 'smooth',
                        block: 'center'
                    });
                }
            });
        });
    </script>
</body>
</html>
